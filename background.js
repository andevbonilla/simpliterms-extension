    // chrome.tabs.onActivated.addListener(function(activeInfo) {
    //     const tabId = activeInfo.tabId;
    //     console.log(tabId, "the tab idddddddddd");
      
    //     chrome.tabs.get(tabId, function(tab) {
    //       if (tab && tab.url) {
    //         // Envía un mensaje al script de contenido actual con la URL
    //         console.log(tab.url, "urlllllllllll");
    //         chrome.tabs.sendMessage(tabId, { url: tab.url, tabId });
    //       }
    //     });
    // });


